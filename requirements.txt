# Install with: pip install -r requirements.txt
#
# For torch: adjust the --extra-index-url to match your cluster's CUDA version.
# Check with: nvcc --version or nvidia-smi
#   CUDA 12.1 → https://download.pytorch.org/whl/cu121
#   CUDA 12.4 → https://download.pytorch.org/whl/cu124
#   CUDA 13.0 → https://download.pytorch.org/whl/cu130  (local machine)
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.6.0
accelerate==1.14.0

# transformers 5.x is required for the qwen3_5 judge model (config.JUDGE_MODEL_NAME,
# e.g. Qwen/Qwen3.5-4B) — earlier 4.57.x does not know that architecture.
# Note: since 4.57.x, Qwen2DecoderLayer.forward() returns a bare tensor (not a
# tuple); model_utils._make_compat_fwd_hook handles both shapes, so the stage-01
# activation extraction still works. Re-verify that hook if you change this pin.
transformers==5.13.0
tokenizers==0.22.2
huggingface-hub==1.22.0

# Utilities
tqdm==4.68.3
numpy==2.5.0
matplotlib==3.11.0
protobuf==6.33.6
requests
